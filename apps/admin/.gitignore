# Miniapp Flutter — apps/admin. Estándar de `flutter create`.
#
# Plataformas: `web/` existe desde el change panel-entrar (2026-09-16).
# `android/` entra en el hito 2, con la APK.

.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# Lo que el IDE genera al lado del proyecto.
.idea/
*.iml
