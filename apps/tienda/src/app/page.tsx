// Andamio. Las paginas reales (/vinos, /vinos/[slug], /carrito, /checkout,
// /orden/[numero]) son el paso 5 de ARQUITECTURA §12 y se construyen
// bottom-up -- del componente hoja a la pagina, nunca al reves-- despues de
// /disenio.
export default function Home() {
  return <main>bouquet</main>;
}
